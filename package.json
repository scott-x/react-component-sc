{
  "name": "babel",
  "version": "1.0.0",
  "description": "npm init -y npm install babel-cli babel-register babel-preset-es2015 babel-preset-stage-2 --save-dev",
  "main": "index.js",
  "scripts": {
    "start": "node bin/dev",
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir dist && babel src -s -d dist",
    "production": "npm run build && node bin/production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.26.0"
  }
}
